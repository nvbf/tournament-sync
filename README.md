# Tournament sync

## Cloud Run and Cloud Build pipelines

Cloud Build pipeline files and setup instructions are in:

- cloudbuild/main-build.yaml
- cloudbuild/dev-deploy.yaml
- cloudbuild/release-dispatch.yaml
- cloudbuild/beta-deploy.yaml
- cloudbuild/prod-deploy.yaml
- cloudbuild/SETUP.md

Bootstrap script:

- setup.sh